#include "FragTrap.hpp"

int main()
{
    // ScavTrap scav("Elio");
    // scav.attack("Pippo");
    // scav.takeDamage(10);
    // scav.beRepaired(10);
    // scav.guardGate();

    FragTrap frag("Enzo");
    frag.attack("Pippo");
    frag.takeDamage(10);
    frag.beRepaired(10);
    frag.highFivesGuys();
    return 0;
}
